#!/bin/bash

set -e

if [ "$NAME" = "nginx" ]; then
    echo "---> Installing nginx application source"
    ${STI_SCRIPTS_PATH}/nginx-assemble

else
    echo "---> Installing web application source"
    ${STI_SCRIPTS_PATH}/webapp-assemble
fi
