#!/bin/bash

set -e

if [ "$NAME" = "nginx" ]; then
    echo "---> Installing nginx application source"
    ./nginx-assemble

else
    echo "---> Installing web application source"
    ./webapp-assemble
fi
